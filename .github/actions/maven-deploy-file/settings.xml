<settings>
    <servers>
        <server>
            <id>authenticated-maven-repository</id>
            <username>${env.MAVEN_USERNAME}</username>
            <password>${env.MAVEN_PASSWORD}</password>
        </server>
    </servers>
</settings>
