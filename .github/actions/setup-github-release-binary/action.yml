name: 'Setup tool'
description: 'Set up a specific version of a tool and add it to the PATH.'
inputs:
  repo:
    description: 'Repository of tool'
    required: true
  version:
    description: 'Version of tool'
    required: true
  template:
    description: 'Template for download url, can use VERSION/NAME/OS/ARCH env vars'
    required: false
    default: 'v${VERSION}/${NAME}_${VERSION}_${OS}_${ARCH}.tar.gz'
runs:
  using: "composite"
  steps:
    - shell: bash
      env:
        REPO: ${{ inputs.repo }}
        VERSION: ${{ inputs.version }}
        TEMPLATE: ${{ inputs.template }}
      run: |
        TOOL_NAME=${TOOL_REPO##*\/}
        TOOL_OS=$(uname | tr '[:upper:]' '[:lower:]')
        TOOL_ARCH=$(uname -m)
        TOOL_URL=https://github.com/$TOOL_REPO/releases/download/$(eval echo $TEMPLATE)
        echo "Downloading $TOOL_URL ..."
        curl -fsSL $TOOL_URL | tar xz $TOOL_NAME
        sudo mv $TOOL_NAME /usr/local/bin/
        echo $($TOOL_NAME --version)
