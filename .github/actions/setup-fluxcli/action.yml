name: 'Setup Flux CLI'
description: 'Set up a specific version of Flux CLI and add it to the PATH.'
inputs:
  version:
    description: 'Version of Flux CLI'
    required: false
    default: 2.9.2
runs:
  using: "composite"
  steps:
    - uses: Alfresco/alfresco-build-tools/.github/actions/setup-github-release-binary@7bd8a36bf171e41f571645e8e66d3252cb030625
      with:
        binary_name: flux
        repo: fluxcd/flux2
        version: ${{ inputs.version }}
