name: 'Setup Flux CLI'
description: 'Set up a specific version of Flux CLI and add it to the PATH.'
inputs:
  version:
    description: 'Version of Flux CLI'
    required: false
    default: 2.9.0
runs:
  using: "composite"
  steps:
    - uses: Alfresco/alfresco-build-tools/.github/actions/setup-github-release-binary@1365eba6d741036a9164212d2ff52523b8c047ad
      with:
        binary_name: flux
        repo: fluxcd/flux2
        version: ${{ inputs.version }}
