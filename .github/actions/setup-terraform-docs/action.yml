name: 'Setup terraform-docs'
description: 'Setup terraform-docs and add it to the PATH.'
inputs:
  version:
    description: 'Version of terraform-docs'
    required: false
    default: 0.24.0
runs:
  using: "composite"
  steps:
    - uses: Alfresco/alfresco-build-tools/.github/actions/setup-github-release-binary@f53a178656dbd7b9ecc4761017a9904066b61e38
      with:
        repo: terraform-docs/terraform-docs
        version: ${{ inputs.version }}
        url_template: 'v${VERSION}/${NAME}-v${VERSION}-${OS}-${ARCH}.tar.gz'
