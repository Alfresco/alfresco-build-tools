name: pre-commit

on:
  workflow_call:
    inputs:
      python-version:
        required: false
        type: string
        default: "3.9"

jobs:
  example_job:
    name: Run pre-commit
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/pre-commit
        with:
          python-version: ${{ inputs.python-version }}
