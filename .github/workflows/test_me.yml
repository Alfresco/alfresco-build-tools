#Deploy SNAPSHOTS artifacts once a day to Nexus, so they can be consumed by Veracode
name: test workflow

on:
  workflow_dispatch:
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run notification
        uses: ./.github/actions/run-time-notification
