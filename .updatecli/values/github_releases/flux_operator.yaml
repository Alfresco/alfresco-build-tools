github:
  prefix: 'build(deps)'
  message: 'bump flux-operator to {{ source "lastRelease" }}'

github_release:
  owner: 'controlplaneio-fluxcd'
  repo: 'flux-operator'

target:
  file: '.github/actions/flux-operator-bootstrap/action.yml'
  key: '$.inputs.operator-version.default'
  trimprefix: 'v'
