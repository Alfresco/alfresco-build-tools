language: python
env:
  global:
    - CHECKOV_VERSION=${CHECKOV_VERSION:-2.0.795}
jobs:
  include:
    - name: checkov
      stage: lint
      language: python
      python: 3.9
      install:
        - pip install checkov==$CHECKOV_VERSION
