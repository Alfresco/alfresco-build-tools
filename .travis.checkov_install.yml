language: python
env:
  global:
    - CHECKOV_VERSION=${CHECKOV_VERSION:-2.0.795}
jobs:
  include:
    - name: pre-commit
      stage: lint
      language: python
      python: 3.9
      before_install:
        - pip install checkov==$CHECKOV_VERSION
